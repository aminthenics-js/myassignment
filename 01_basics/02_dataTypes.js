// Create these variables and check their types:

// 1. Your favorite book title (string)
let favBook = "Linux Command line";

// 2. Your birth year (number) 
let birtYear = 2005;

// 3. Whether you like programming (boolean)
let likeProgramming = true;

// 4. A variable with no value (undefined)
let State;

// 5. An intentionally empty value (null)
let theTemp = null;


console.log(typeof favBook);
console.log(typeof birtYear);
console.log(typeof likeProgramming);
console.log(typeof State);
console.log(typeof theTemp);



// 1. Create a string containing a number: "2025"
let notNum = "2025";

// 2. Create a number: 42
let realNum = 42;

// 3. Try adding them together: "2025" + 42
let add = "2025" + 42;  // (string concatenation)

// 4. Try subtracting them: "2025" - 42
let substract = "2025" - 42;  // (mathematical subtraction)

// 5. Check the types of the results
console.log(add);
console.log(substract);

// + can mean addition OR string concatenation
// - only means mathematical subtraction, so it converts to numbers



  let userName = "Aminur";
  let  age = 20;
  let  isOnline = true;
  let  lastLogIn = null;
  let userEmail;

 console.log(typeof userName);
 console.log(typeof age);
 console.log(typeof isOnline);
 console.log(typeof lastLogIn);
 console.log(typeof userEmail);
  
 console.table([userName, age, isOnline, lastLogIn, userEmail])


 
 console.log(typeof null);
 console.log(typeof undefined);

 let isBoolean = true + 1;
 let boolean = false + 1;

 console.log(isBoolean);
 console.log(boolean);
 